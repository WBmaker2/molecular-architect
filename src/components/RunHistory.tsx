import type { Molecule, RunRecord } from '../domain/moleculeTypes';
interface Props { records: RunRecord[]; molecules: Molecule[]; }
export function RunHistory({ records, molecules }: Props) { return <section className="history" data-od-id="run-history"><div className="section-kicker">04 / SESSION LOG</div><h2>실행 기록</h2>{records.length === 0 ? <p className="muted">구조를 기록하면 처음 예측과 결과가 여기에 남습니다.</p> : <div className="history-list">{records.map((record, index) => { const m = molecules.find((item) => item.id === record.moleculeId); return <div className="history-item" key={`${record.timestamp}-${index}`}><b>{m?.formula}</b><span>예측 {record.prediction === 'zero' ? '비극성' : '극성'} → 결과 {record.result === 'zero' ? '비극성' : '극성'}</span><small>{record.geometry} · 근거 {record.evidence.length}개</small></div>; })}</div>}<p className="model-note">세션 기록은 이 화면을 닫으면 사라집니다. 자동 채점·계정 저장 없음.</p></section>; }
