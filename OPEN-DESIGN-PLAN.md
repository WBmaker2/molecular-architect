# 분자의 건축가 · OpenDesign 구현 계획

> 작성일: 2026-09-13  
> 기준 문서: `00-shared-design-principles.md`, `08-molecular-architect.md`  
> 상태: 구현 착수용 계획 v1.0

## 1. 구현 목표

고등학교 화학 학습자가 검수 대상 12개 분자 중 하나를 선택하고, 결합 전자영역과 고립 전자쌍을 예측한 뒤 `구조 적용`을 실행하여 VSEPR 이상화 기하·결합 쌍극자·분자 전체 극성을 비교하는 1차시용 정적 웹앱을 구현한다.

앱은 범용 분자 계산기가 아니다. 12개 고정 카탈로그와 검수된 이상화 좌표만 사용하고, 실제 전자밀도·양자화학·반응성·정량 쌍극자 예측을 하지 않는다.

## 2. OpenDesign 방향

- 라우팅: `open-design-web-prototype`
- 기반: OpenDesign 시드의 6개 색 토큰, 절제된 단일 강조색, 얇은 경계, 넓은 여백, serif 제목·sans 본문·mono 수치 체계를 학습 앱 셸에 적용한다.
- 디자인 시스템: 별도 `DESIGN.md`가 없으므로 번들 `Neutral Modern`을 교육용 밝은 cyan 계열로 매핑한다.
- 화면 리듬: `상단 상태 → 분자 선택 → 탐구 무대 → 조작·근거 → 결과·모델 범위`
- 장식 이미지: 밝은 분자 설계 스튜디오 배경 1개만 사용하며, 실제 분자 구조와 극성 근거는 Three.js/HTML/SVG 계산 렌더링으로만 표현한다.
- 모든 주요 화면 영역에 `data-od-id`를 부여한다.

## 3. 학습 흐름

1. CO₂와 H₂O 중 하나의 전체 극성을 먼저 예측한다.
2. 12개 카드에서 분자를 고른다.
3. 결합 전자영역 수와 고립 전자쌍 수를 조절한다.
4. 현재 단계의 필수 행동인 `구조 적용`을 실행한다.
5. 3D 구조를 정면·측면·위 시점으로 관찰하고 결합 쌍극자/대칭 가이드를 켠다.
6. 전자영역 기하와 분자 기하를 구분하여 확인한다.
7. 결합 극성과 전체 극성을 분리해 근거 문장을 선택한다.
8. 실행 기록에서 처음 예측과 결과를 비교하고 모델 범위 문장을 완성한다.

첫 오답은 막지 않고 전자영역 수, 원자 위치 수, 고립 전자쌍의 차이를 보여 준 뒤 재시도하게 한다. 목록 밖 화학식 입력은 실행하지 않고 MVP 범위 안내를 표시한다.

## 4. 화면 구성

### 상단 바

- 브랜드 `분자의 건축가`
- 현재 과제 단계와 진행률
- `VSEPR 이상화` 배지
- 작은 `업데이트 내역` 버튼과 실제 날짜별 변경 모달

### 탐구 본문

- 상단/모바일 우선: 선택 분자의 이름·화학식·모형 한계
- 중앙: Three.js 구-막대 구조, 결합 쌍극자 화살표, 순벡터, 대칭 가이드
- WebGL 실패 시: 동일 좌표 기반 SVG/HTML 2D 대체 화면과 결과 표
- 시점: 정면·측면·위 프리셋, 마우스/터치 회전. 기본 자동 회전 없음

### 조작·근거 패널

- 12분자 카드 그리드(모바일에서도 줄바꿈, 가로 스크롤 없음)
- 결합 전자영역/고립 전자쌍 stepper
- 결합 쌍극자와 대칭 가이드 독립 토글
- `구조 적용` 한 개에만 `gi-pulse` 강조
- 예측 선택, 근거 문장 선택, 재시도 안내

### 결과·기록

- 입체수, 전자영역 기하, 분자 기하, 결합 극성, 전체 극성
- `δ+ → δ−` 결합 극성 화살표와 `− → +` 물리학적 쌍극자 모멘트 방향을 서로 다른 기호/설명으로 구분
- 세션 내 실행 기록과 비교 대상 바로가기
- VSEPR 이상화, 상대 길이, 문헌 대조 미완료 상태를 상시 표시

## 5. 기술 구조

React + TypeScript + Vite + Three.js로 구성하고, 모든 기능 파일을 500줄 미만으로 유지한다.

```text
src/
  domain/moleculeTypes.ts
  engine/vsepr.ts
  engine/dipoleQualitative.ts
  scenarios/moleculeCatalog.ts
  renderers/MoleculeScene.tsx
  components/AppHeader.tsx
  components/MoleculeCardGrid.tsx
  components/BuilderControls.tsx
  components/ResultsPanel.tsx
  components/RunHistory.tsx
  components/UpdateLog.tsx
  App.tsx
  styles.css
```

- 엔진은 Three.js/React에서 분리해 순수 함수 테스트가 가능해야 한다.
- 카탈로그는 12개 항목의 중심 원자, 전자영역, 고립 전자쌍, 이상화 좌표, 결합/전체 극성, 검수 상태를 가진다.
- 모든 상태는 세션 메모리에만 두고 계정·영구 저장·외부 AI 호출을 추가하지 않는다.
- 정적 배포 가능한 결과물로 만든다.

## 6. 상호작용·접근성

- 카드, stepper, 토글, 시점 프리셋, 모달은 키보드로 조작 가능해야 한다.
- 실행 후 결과 제목으로 프로그램 초점을 이동하고 다음 필수 행동이 보이게 한다.
- 색상 외에 원소 기호, 선 종류, 화살표 머리, 텍스트 라벨을 병기한다.
- 320px에서 핵심 조작의 가로 넘침이 없어야 한다.
- `prefers-reduced-motion`에서는 gi-pulse를 정적 테두리 강조로 바꾸고 모든 불필요한 전환을 제거한다.
- 밝은 테마로 고정한다.
- VoiceOver 구현·검증은 범위에서 제외한다.

## 7. 검증 계획

### 엔진

- V1 CO₂: 선형, 결합 극성 있음, 전체 비극성
- V2 H₂O: 굽은형, 결합·전체 극성
- V3 BF₃: 삼각 평면, 전체 비극성
- V4 NH₃: 삼각뿔형, 전체 극성
- V5 CH₄: 정사면체, 전체 비극성
- V6 SiCl₄: 정사면체, 전체 비극성
- V7 ClF₃: T자형, 전체 극성
- V8 XeF₄: 평면 정사각형, 전체 비극성
- V9 목록 밖 입력: 실행 중단과 범위 안내

### 통합·학생 흐름

- 예측 → 설정 → 적용 → 관찰 → 근거 선택 → 재시도 → 비교 기록 흐름
- 분자 변경 시 입력·3D·결과·근거가 함께 갱신되는지 확인
- 잘못된 전자영역 입력에서 정답을 노출하기 전에 원인 단서를 제공하는지 확인

### 브라우저

- 1280px, 360px, 320px에서 레이아웃 및 `scrollWidth === clientWidth`
- 키보드 초점 순서, 결과 초점 이동, Escape 모달 닫기
- 모션 감소 모드
- WebGL 미지원 대체 화면

검증 결과는 엔진/빌드, 로컬 브라우저, 공개 배포, 교과 수용성을 분리하여 보고한다.

## 8. 이번 작업 범위

포함: 프로젝트 초기화, 12개 카탈로그, 독립 VSEPR/쌍극자 엔진, 반응형 학습 UI, Three.js 구조, 2D 대체, 업데이트 내역, 장식 이미지 1개, 엔진 테스트, 빌드 및 로컬 브라우저 QA.

제외: 배포, Git 커밋·푸시, HVC 등록/정적 갤러리 동기화, 실제 교과 검수 완료 선언, 정량 쌍극자, 목록 밖 자동 생성, VoiceOver.

## 9. 2026-09-14 QA·성능·릴리스 실행 계획

사용자의 후속 승인에 따라 아래 단계를 순서대로 수행한다.

1. 12분자 전체 QA
   - 카탈로그의 전자영역·분자 기하·결합 극성 방향·전체 극성 기대값을 독립 엔진 테스트로 확인한다.
   - 12개 카드를 실제 브라우저에서 순회하며 선택 상태, 결과 갱신, WebGL 렌더링, 오류 콘솔을 확인한다.
   - CO₂와 H₂O의 예측→설정→적용→근거→기록 흐름, 잘못된 설정 복구, 목록 밖 입력을 확인한다.
   - 1280px·360px·320px 및 모션 감소, 키보드 초점, 2D 대체 화면을 확인한다. VoiceOver는 제외한다.
2. 성능 최적화
   - Three.js 렌더러를 지연 로딩/청크 분리하여 초기 앱 청크를 줄인다.
   - 생성 배경 이미지를 웹 전달용 형식으로 최적화하고 실제 디코딩·표시를 확인한다.
   - 최적화 전후 빌드 크기를 기록하고 모든 테스트와 브라우저 회귀 검사를 다시 실행한다.
3. GitHub 릴리스
   - 현재 폴더의 저장소·원격 상태를 확인하고, 없으면 `molecular-architect` 저장소를 생성한다.
   - GitHub Pages용 base와 Actions 워크플로를 추가하고 릴리스에 필요한 파일만 커밋한다.
   - `main` 푸시, Pages 워크플로 성공, 공개 URL의 자산·학생 흐름·320px 무가로스크롤을 별도로 확인한다.
4. HVC 관리자 등록
   - 공개 앱을 먼저 읽고 첫 화면의 실제 스크린샷을 썸네일로 준비한다.
   - 기존 항목·태그 어휘를 확인한 뒤 중복이 없을 때 생성하고 저장 후 카드·편집 상태를 재확인한다.
   - 관리자 DB 등록까지만 수행한다. 정적 갤러리 동기화는 실행하지 않는다.

각 단계의 성공 증거는 자동 테스트, 로컬 브라우저, GitHub/Pages, HVC 관리자 저장 상태로 분리하여 보고한다.
